"use client";

import React from "react";
import styled from "styled-components";

export default function MainContainer() {
  return (
    <Wrap>
      <div>Pretendard 프리텐다드</div>
    </Wrap>
  );
}

const Wrap = styled.div`
  width: 100%;
  display: flex;
  justify-content: center;
  padding: 72px 0 0 0;
`;
